<h1>My tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <% if task[:completed] === true %>
      <li>[X] <%= task[:title] %></li>
    <% else %>
      <li>[ ] <%= task[:title] %></li>
    <% end %>
  <% end %>
</ul>
